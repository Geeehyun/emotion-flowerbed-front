# 의존성 (Docker 내부에서 npm ci로 새로 설치)
node_modules
npm-debug.log*

# Git 관련
.git
.gitignore
.gitattributes

# 빌드 결과물 (Docker 내부에서 새로 빌드)
dist
dist-ssr

# 환경 변수 파일 (빌드 시 ARG로 주입)
.env
.env.local
.env.*.local

# 개발 도구 설정
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 로그 파일
*.log

# 운영체제 파일
.DS_Store
Thumbs.db

# 테스트 관련
coverage
.nyc_output

# 문서 및 학습 자료
README.md
CLAUDE.md
DEPLOYMENT_TODO.md
study

# Docker 관련 (중복 방지)
Dockerfile
docker-compose.yml
.dockerignore
